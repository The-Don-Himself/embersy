
import { compare } from 'frontend/helpers/compare';
import { module, test } from 'qunit';

module('Unit | Helper | compare');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = compare([42]);
  assert.ok(result);
});

